# halqe — Python dependencies (Django 6 + django-ninja + Postgres).
#
# Pinned to EXACT versions (==) for a deterministic, reproducible build: these are
# the versions the full test suite is green against (source: `pip freeze` in the
# known-good .venv). To bump a dependency: change the pin, re-run the full suite,
# then commit. Never widen to >= — that reintroduces non-deterministic builds
# (the previous `Django>=5.0` allowed an unintended 5.x / 6.x major jump).
#
# Transitive dependencies are locked at BUILD time by the offline wheel artifact
# (halqe/wheels/, populated by `pip download` per the Dockerfile) — that directory
# is the build-time lock; this file is the source-of-truth for direct deps.
#
# gunicorn (production WSGI server) is pinned in the Dockerfile, not here, because
# it is a deploy-only dependency and is not installed in the dev/test venv.
#
# NOTE: pytest / pytest-django are test-only but are still installed into the
# image today (the Dockerfile installs `-r requirements.txt`). Splitting prod vs
# dev deps (a requirements-dev.txt) is a separate future cleanup.

# -- Runtime --
Django==6.0.6
django-ninja==1.6.2
django-cors-headers==4.9.0
psycopg[binary]==3.3.4
bcrypt==5.0.0
PyJWT==2.13.0

# -- Test --
pytest==9.1.1
pytest-django==4.12.0
