interface:
  display_name: "Clinic Managment"
  short_description: "Repo-specific patterns and workflows for clinic_managment"
  default_prompt: "Use the clinic_managment repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true