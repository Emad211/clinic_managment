# ── Docker offline-build wheels (arch-specific, regenerable; never commit) ──
halqe/wheels/

# ── Python venvs / caches ──
# (per-app venvs are also covered by nested .gitignore files; this is the
#  repo-wide safety net. The removed platform/ and ai_service/ trees are gone.)
**/.venv/
**/__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/

# ── Local env & secrets ── (NOTE: .env.example IS committed — see halqe step 22)
.env
**/.env

# ── Research / scratch ──
tmp_*.txt
# one-off scratch artifacts (kept on disk, never committed)
*.zip
specialist_clinic/kave_tmp.html
specialist_clinic/docs/migration_tools/devi04_out/

# ── Editor / OS ──
.DS_Store
Thumbs.db

# ── Scheduler lock ──
.claude/scheduled_tasks.lock
