# A10 Gate Trigger

This marker synchronizes PR #A10 after the temporary exact-head release workflow is installed. It is removed by the successful release commit together with all deterministic finalizers.
