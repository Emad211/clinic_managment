# A10 release scope

Only `specialist_clinic` is changed. `webapp` and the production accounting schema/data remain outside the write boundary. The release gate verifies this before publishing the tested branch tree.
